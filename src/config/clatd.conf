# CLAT (464XLAT / RFC 6877) para o modem 4G XMM7360 em rede IPv6-only.
# Permite acessar sites/serviços que só têm IPv4, traduzindo via o NAT64
# da operadora no prefixo well-known 64:ff9b::/96 (agnóstico de operadora).
plat-dev=wwan0
plat-prefix=64:ff9b::/96
v4-conncheck-enable=0
